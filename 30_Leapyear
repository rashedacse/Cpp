// Leap year.

#include<iostream>
using namespace std;
int main()
{
    int year;
    cout<<"Enter year:\n";
    cin>>year;
    if((year%4==0)&&(year%100==0)||(year%400==0))
    {
        cout<<"Entered year is leap year.";
    }
    else{
        cout<<"NOt! leap year.";
    }
}
