/*/ 1+2+3+4+......+n=?
#include<iostream>
#include<math.h>
using namespace std;
int main()
{ int i,n,sum=0;
  cout<<"Enetr num of elements: "; cin>>n;
  for(i=1;i<=n;i++)
  sum=sum+i; cout<<"Thr sumetion = "<<sum;
}*/
/*/1*1+2*2+3+4+......+n*n=?
#include<iostream>
#include<math.h>
using namespace std;
int main()
{ int i,n,sum=0;
  cout<<"Enetr num of elements: "; cin>>n;
  for(i=1;i<=n;i++)
  sum=sum+i*i; cout<<"Thr sumetion = "<<sum;
}*/
//1+3+5+......+n=?
#include<iostream>
#include<math.h>
using namespace std;
int main()
{ int i,n,sum=0;
  cout<<"Enetr num of elements: "; cin>>n;
  for(i=1;i<=n;i=i+2)
  sum=sum+i; cout<<"Thr sumetion = "<<sum;
}
/*/1,2,3,,,,,n.find even & odd.
#include<iostream>
#include<math.h>
using namespace std;
int main()
{ int i,n,sum=0,even=0,odd=0;
  cout<<"Enetr num of elements: "; cin>>n;
  for(i=1;i<=n;i++)
  { if(i%2==0) { even=even+i; }
    else  {odd=odd+i;}
  }
    cout<<"even number= "<<even;
    cout<<"odd number= "<<odd;
}*/


