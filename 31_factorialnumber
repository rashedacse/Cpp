// Factorial number.

#include<iostream>
using namespace std;
int main()
{
    int n,k;
    cout<<"Enter a number:  ";
    cin>>n;
    int long f=1;
    for(k=1;k<=n;k++)
    {
        f=f*k;
    }
    cout<<"The factorial is =  "<<f;
}

/* #include<iostream>
using namespace std;
long fact(long n);
int main()
{
    int n,f;
    cout<<"Enter a number:  ";
    cin>>n;
    f=fact(n);
    cout<<"The factorial is =  "<<f;
    return 0;
}
long fact(long n)
{
    if(n==1)
    {return 1;
    }
    else{
        return (n*fact(n-1));
    }
}*/
